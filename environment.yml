name: BCI
channels:
  - defaults
dependencies:
  - python=3.8
  - pip
  - matplotlib
  - pandas
  - scipy
  - numpy
  - scikit-learn
  - pip:
      - explorepy>=1.6.0
      - pygame
      - imblearn
      - mne
prefix: E:\miniconda3_venvs\BCI
