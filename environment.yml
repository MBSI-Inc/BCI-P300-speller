name: BCI
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.8
  - pip
  - matplotlib
  - pandas
  - scipy
  - numpy
  - scikit-learn
  - liblsl
  - pip:
      - git+https://github.com/Mentalab-hub/explorepy.git@user-req-rec_struct
      - pygame
      - imblearn
      - mne
      - autopep8
prefix: E:\miniconda3_venvs\BCI
